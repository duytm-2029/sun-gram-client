/*
 * postWrite Messages
 *
 * This contains all the text for the postWrite container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.postWrite';

export default defineMessages({
  postCancelButton: {
    id: `${scope}.posts`,
    defaultMessage: 'Cancel',
  },
  updateButton: {
    id: `${scope}.posts`,
    defaultMessage: 'Update',
  },
  postButton: {
    id: `${scope}.posts`,
    defaultMessage: 'Post',
  },
  cancelButton: {
    id: `${scope}.posts`,
    defaultMessage: 'Cancel',
  },
});
