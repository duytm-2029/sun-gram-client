/*
 * CommentGroup Messages
 *
 * This contains all the text for the CommentGroup component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.components.commentgroup';

export default defineMessages({
  commentPostButton: {
    id: `${scope}.addCommentPlaceholder`,
    defaultMessage: 'Post',
  },
});
